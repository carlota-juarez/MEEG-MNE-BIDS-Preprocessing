name: app-mne-bids-preprocessing
channels:
  - conda-forge
  - defaults 
dependencies:
  - python=3.11
  - pip
  - pip:
      - numpy<2.0.0
      - scipy
      - matplotlib
      - scikit-learn
      - mne
      - mne-bids
      - mne-bids-pipeline==1.10.1